# Cloudflare Pages production build value. Keep preview builds isolated from production data.
VITE_API_BASE_URL=https://api.oddpath.eluthira.com

# Optional: set only when direct browser R2 transfers are enabled. Replace the
# placeholder with the exact 32-character account id; wildcards and paths fail the build.
# VITE_R2_ENDPOINT=https://<cloudflare-account-id>.eu.r2.cloudflarestorage.com
